import { EmployeeAttribute } from './employee-attribute.model';

describe('EmployeeAttribute', () => {
  it('should create an instance', () => {
    expect(new EmployeeAttribute()).toBeTruthy();
  });
});
